# 문자열

---
## 1. 문자 찾기
### 문법 설명
`str.toLowerCase()`: 문자열을 소문자로 변환</br>
`Character.toLowerCase(c)`: c 단어를 소문자로 변환</br>
`str.charAt(i)`: str의 i번째 단어를 가져옴</br>

## 2. 대소문자 변환
### 문법 설명
`str.toCharArray()`: str을 이루는 문자들로 배열을 만듦 </br>
